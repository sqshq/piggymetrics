hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000

spring:
  data:
    mongodb:
      database: piggymetrics
      port: 0

rates:
  url: http://api.fixer.io